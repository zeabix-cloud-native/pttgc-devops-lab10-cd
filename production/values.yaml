# Default values for ..
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: nginx
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""
imagePullSecrets: []
userService:
  enabled: false
  name: "user-service"
  image: "pttgcdevopsarc13.azurecr.io/user-service"
  tag: "bddabd116e8d697f14775b3a193fac9402a293b7"
  pullPolicy: IfNotPresent
  servicePort: 8080
  resources:
    requests:
      memory: 1Gi
      cpu: 500m
    limits:
      memory: 1.5G
      cpu: 1
ingress:
  enabled: false
  className: ""
  annotations: {}
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local
